import * as React from "react";
import { useState } from "react";
import axios from "axios";
import { Modal, ModalBody, ModalFooter } from "./Modal/baseModal";

export const AddBookItem = () => {

  return (
      <Modal>
        <ModalBody>

        </ModalBody>

        <ModalFooter>
        </ModalFooter>
      </Modal>
  );
};
